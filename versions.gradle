def isRemote(){
    def what = false
    if(project.hasProperty("remoteDep")){
        what = remoteDep.toBoolean()
    }
    return what
}

ext{
    isRemote=this.&isRemote
}

project.ext{
    remoteDep=false
    core_logger_version="1.0.5-rc1"
    android_logger_version="1.0.0-rc1"
}

