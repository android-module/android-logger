def isRemote(){
    def what = false
    if(project.hasProperty("remoteDep")){
        what = remoteDep.toBoolean()
    }
    return what
}

ext{
    isRemote=this.&isRemote
}

project.ext{
    remoteDep=true
    core_logger_version="1.0.6-rc1"
    android_logger_version="1.0.6-rc1"
}

